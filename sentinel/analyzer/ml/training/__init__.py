"""Training use cases — orchestrate feature extraction, model fitting,
calibration, and ensemble assembly.

Modules here depend on the domain layer (config, metrics, constants)
and on the model factories, but never on persistence or the façade.
"""
