"""Prediction use case — feature vector → MLPrediction.

Depends on domain, features, and (transitively) the ensemble module.
No training, no persistence, no sklearn-outside-ensemble dependencies.
"""
